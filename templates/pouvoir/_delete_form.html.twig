<form method="post" action="{{ path('app_pouvoir_delete', {'id': pouvoir.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pouvoir.id) }}">
    <button class="btn">Delete</button>
</form>
