<form method="post" action="{{ path('app_super_hero_delete', {'id': super_hero.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ super_hero.id) }}">
    <button class="btn">Delete</button>
</form>
