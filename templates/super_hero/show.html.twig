{% extends 'base.html.twig' %}

{% block title %}SuperHero{% endblock %}

{% block body %}
    <h1>SuperHero</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ super_hero.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ super_hero.nom }}</td>
            </tr>
            <tr>
                <th>AlterEgo</th>
                <td>{{ super_hero.alterEgo }}</td>
            </tr>
            <tr>
                <th>EstDisponible</th>
                <td>{{ super_hero.estDisponible ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>NiveauEnergie</th>
                <td>{{ super_hero.niveauEnergie }}</td>
            </tr>
            <tr>
                <th>Biographie</th>
                <td>{{ super_hero.biographie }}</td>
            </tr>
            <tr>
                <th>NomImage</th>
                <td>{{ super_hero.nomImage }}</td>
            </tr>
            <tr>
                <th>CreeLe</th>
                <td>{{ super_hero.creeLe ? super_hero.creeLe|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_super_hero_index') }}">back to list</a>

    <a href="{{ path('app_super_hero_edit', {'id': super_hero.id}) }}">edit</a>

    {{ include('super_hero/_delete_form.html.twig') }}
{% endblock %}
