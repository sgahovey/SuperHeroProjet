<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}SuperHero Hub{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="icon" href="{{ asset('favicon.ico') }}" type="image/x-icon">
    <style>
        /* Styles généraux */
        .min-h-screen {
            min-height: 100vh;
        }
        .text-shadow {
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.7);
        }
        .text-gold {
            color: #FFD700;
        }
        .btn-marvel {
            background-color: #e62429;
            color: white;
            transition: background-color 0.3s ease, transform 0.2s ease;
        }
        .btn-marvel:hover {
            background-color: #d91e24;
            transform: translateY(-2px);
        }
        .bg-marvel-red {
            background-color: #e62429;
        }
        .card-hover:hover {
            transform: translateY(-5px);
            transition: transform 0.2s ease-in-out;
        }
        .container-dark {
            background-color: #121212;
            color: #f1f1f1;
            padding: 2rem;
            border-radius: 8px;
        }
    </style>
</head>
<body class="bg-dark text-light">
    <!-- Navbar -->
    <header class="navbar navbar-expand-lg navbar-dark bg-marvel-red shadow">
        <div class="container-fluid">
            <a class="navbar-brand fw-bold fs-4 text-gold" href="{{ path('app_index') }}" aria-label="Accueil">SuperHero</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.attributes.get('_route') == 'app_index' %}active{% endif %}" href="{{ path('app_index') }}">Accueil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.attributes.get('_route') == 'app_super_hero_index' %}active{% endif %}" href="{{ path('app_super_hero_index') }}">Super Héros</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.attributes.get('_route') == 'app_pouvoir_index' %}active{% endif %}" href="{{ path('app_pouvoir_index') }}">Pouvoirs</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.attributes.get('_route') == 'app_equipe_index' %}active{% endif %}" href="{{ path('app_equipe_index') }}">Equipes</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.attributes.get('_route') == 'app_mission_index' %}active{% endif %}" href="{{ path('app_mission_index') }}">Missions</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.attributes.get('_route') == 'app_dashboard' %}active{% endif %}" href="{{ path('app_dashboard') }}">Dashboard</a>
                    </li>
                </ul>
            </div>
        </div>
    </header>

    <!-- Main Content -->
    <main class="container py-5">
        {% block body %}{% endblock %}
    </main>

    <!-- Footer -->
    <footer class="text-center bg-marvel-red text-light py-3">
        <p>&copy; {{ "now"|date("Y") }} SuperHero. Tous droits réservés.</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
